# X API (App-only Bearer for public election monitoring — highest rate limits)
X_BEARER_TOKEN=your_x_bearer_token_here

# xAI Grok — use the OpenAI-compatible endpoint
XAI_API_KEY=your_xai_api_key_here

# Shared secret for Vercel Cron + manual ingest (set a strong random value)
INGEST_SECRET=colombia-election-day-2026-CHANGE-ME

# Optional: for future OAuth analyst accounts
# NEXT_PUBLIC_X_CLIENT_ID=...
